const BaseWallet = require('../../../common/blockchain/base-wallet');

class DashWallet extends BaseWallet {

    constructor(...props) {
        super(...props);
    }

}

module.exports = DashWallet;
