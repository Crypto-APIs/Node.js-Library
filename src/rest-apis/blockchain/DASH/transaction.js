const BaseTransaction = require('../../../common/blockchain/base-transaction');

class DashTransaction extends BaseTransaction {

    constructor(...props) {
        super(...props);
    }

}

module.exports = DashTransaction;
