const BaseTransaction = require('../../../common/blockchain/base-transaction');

class DOGETransaction extends BaseTransaction {

    constructor(...props) {
        super(...props);
    }

}

module.exports = DOGETransaction;
